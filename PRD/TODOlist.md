# 对账单系统重构 TODO（面向可配置与可扩展）

## 本轮范围与状态（截至 2026-02-11）

- 本轮目标：优先完成 `目标2（生成引擎规则驱动）`，并已修复“新建对账单缺少上传入口”问题。
- 明确暂不做：审批流相关（原优先级 3）与审计日志相关（原优先级 4）。
- 仍需继续做：`目标1`（对账单主数据从 `statements_db.json` 迁移到数据库）。

状态标记说明：
- `[x]` 已完成
- `[ ]` 待做/进行中
- `[ ] ...（暂不做）` 本轮明确不做

## 0. 复盘结论（现状偏差）

1. 当前系统入口顺序与业务预期不一致：已先做“对账单管理”，但缺少可维护的“字典表/计费项配置中心”。
2. 生成逻辑仍偏硬编码：层级映射、分配比例、补齐策略、告警阈值写死在代码中，变更成本高（已进入规则驱动改造，仍需补齐策略插件化）。
3. 业务明细上传虽已存在，但为“可选”，且未形成“数据来源-计算规则-证据链”闭环（已完成上传强引导与数据源记录基础版）。
4. 缺少“待补齐+审批+依据追溯”的完整流程，审计解释能力不足（审批与审计本轮暂不做）。
5. 缺少模板化能力（分层比例模板、客户策略模板、明细解析模板），无法支撑多客户差异化（已完成字段映射模板与分配规则模板基础版）。

## 1. 产品目标（重排优先级）

1. 先做“计费项字典与规则配置中心”，再做“对账单管理与生成”。
2. 新建对账单必须绑定结算依据：优先业务明细上传；无明细时必须走“人工录入+原因+审批”（本轮仅做到“原因必填”，审批暂不做）。
3. 生成引擎改为“规则驱动”，避免写死逻辑，支持扩展新层级/新计费项/新算法（进行中）。
4. 全流程可审计：每条明细可追溯到来源数据、计算公式、操作人、审批记录（本轮暂不做审计与审批部分）。

## 2. 基于模板文件的关键结论（测试业务使用数据.xlsx）

1. 模板字段结构：`年份`、`月份`、`服务提供方` + 多个业务计费项列（如医院拜访、商业拜访、药房拜访等）。
2. 同一模板天然支持“多行多期”累计，系统应支持按账期过滤并聚合数量。
3. 业务列名与配置表收费项名称可直接映射（已完成“字段映射配置+保存模板”）。
4. 当前测试样本仅 1 行，但系统实现必须支持批量行、历史多月、空值/非数值容错。

## 3. 分阶段实施 TODO

## Phase A（P0）：字典表/计费项配置中心（先完成）

- [ ] 建立“计费项主数据”模型（DB 表）：编码、名称、层级、单位、单价、启停状态、生效时间、税率、是否允许模拟、是否必须项、是否需审批、排序权重。（进行中：已落地核心字段，未覆盖税率/审批标识等）
- [x] 建立“规则参数”模型：层级占比上下限、L4上限金额、尾差处理策略、优先级顺序、客户类型策略。（已落地基础参数：比例、L4上限、补齐顺序、偏差阈值）
- [ ] 建立“模板模型”：`分层比例模板`、`字段映射模板`、`客户策略模板`。（进行中：已完成字段映射模板与规则模板，客户策略模板未完成）
- [ ] 提供配置管理 API：新增/编辑/停用/版本化发布/历史回溯。（进行中：已支持新增/编辑/停用；版本发布/回溯未完成）
- [x] 新增前端“系统设置-计费项配置”页面（列表、编辑、启停、版本对比）。（已完成基础列表/新增/启停/局部编辑）
- [ ] 新增“导入配置（Excel）+ 校验预览 + 一键发布”能力，替代人工改代码。
- [ ] 验收标准：业务人员可在不改代码情况下完成收费项及规则调整，并立即影响新生成账单。（进行中）

## Phase B（P0）：对账单新建与结算依据闭环

- [x] 重构“新建对账单”流程为三步：基础信息 -> 上传/录入结算依据 -> 预览与生成。
- [x] 上传业务明细改为“强引导”（已将上传入口前置到新建页面）。
- [x] 若上传成功：进入自动解析。
- [ ] 若不上传：必须填写“无明细原因”，并走人工补录+审批。（本轮只做“原因必填”；审批暂不做）
- [ ] 增加“明细解析结果页”：字段映射确认、异常行提示、可手工修正映射。（进行中：已完成字段映射确认；异常行明细待补）
- [ ] 增加“数据源记录”模型：保存原文件、解析版本、解析时间、解析人、hash、错误日志。（进行中：已记录来源类型、文件名、行数与匹配统计）
- [x] 验收标准：每张账单都有明确结算依据来源，无法再出现“金额有结果但来源不清楚”。（基础闭环已满足）

## Phase C（P1）：生成引擎规则化（去硬编码）

- [ ] 抽象“分配引擎接口”：输入（目标金额、模板、业务聚合数据、策略）-> 输出（明细、分层汇总、告警）。（进行中：已改为规则模板驱动，接口层待继续抽象）
- [x] 将硬编码项参数化：层级映射、比例阈值、补齐顺序、随机策略、尾差规则、告警阈值。（已去随机并接入规则参数）
- [ ] 提供可替换策略：
- [x] 标准策略（按模板比例）。（已完成 `standard`）
- [ ] 大客户策略（强调L2交付解释）。
- [ ] 小客户策略（控制复杂度）。
- [ ] 增加“可解释计算日志”：每一步分配依据、计算公式、结果快照。
- [ ] 验收标准：新增或调整策略仅需改配置/策略插件，不改主流程代码。（进行中）

## Phase D（P1）：待补齐、模拟与审批流程

- [ ] 建立“待补齐项”机制：缺数据项自动入池，不直接静默模拟。（暂不做）
- [ ] 模拟补齐必须记录：建议值、人工调整值、原因、审批意见、前后差异。（暂不做）
- [ ] 审批流状态机完善：草稿 -> 待审批 -> 已通过/已驳回 -> 已导出，支持多级审批扩展。（暂不做）
- [ ] 对账单详情页增加“查看依据”抽屉：原始业务记录、报告清单、人工填报及审批痕迹。（暂不做）
- [ ] 验收标准：任何一条模拟数据都可追溯“谁在何时为何这样填”。（暂不做）

## Phase E（P2）：导出、审计与稳定性

- [ ] Excel 导出增强：汇总、明细、依据统计、审批记录四个sheet。
- [ ] PDF 导出（可选）模板化。
- [ ] 新增“审计日志”模型：配置变更、数据导入、生成、审批、导出全链路记录。（暂不做）
- [ ] 加入自动化测试：
- [ ] 配置解析单测。
- [ ] 明细模板解析单测（多行/空值/异常列）。
- [ ] 分配引擎单测（金额守恒、边界条件）。
- [ ] API 集成测试（创建/审批/导出流程）。
- [ ] 验收标准：核心流程可回归验证，减少“改一处坏一片”。

## 4. 技术改造清单（建议）

1. 存储层：从 JSON 文件迁移到关系型数据库（建议 PostgreSQL/MySQL），支持版本与审计。（仍待做；本轮只做规则和配置层）
2. 后端分层：`配置中心`、`数据解析`、`规则引擎`、`流程审批` 四模块解耦。（进行中）
3. 前端信息架构：导航顺序改为“系统设置（先） -> 对账单管理（后）”。（已完成）
4. 兼容策略：保留旧接口只读，新增 v2 接口逐步切流。（待做）

## 5. 立即执行的本周任务（建议）

- [ ] Task 1：补齐数据库 schema（计费项、规则模板、数据源、审批、审计日志）。（进行中；审批/审计本轮暂不做）
- [x] Task 2：完成“计费项配置中心”后端 CRUD + 前端页面。（基础版已完成）
- [x] Task 3：完成业务明细上传解析器 v2（字段映射 + 账期过滤 + 解析报告）。（基础版已完成）
- [x] Task 4：重写生成引擎为“配置驱动”，先落地标准模板A。（第一版已完成）
- [ ] Task 5：联调“新建对账单”三步流程并接入审批。（三步已联调，审批暂不做）

## 6. Definition of Done（全局）

1. 不改代码即可新增/调整计费项和规则。（进行中）
2. 每张账单都有可追溯的结算依据与计算过程。（基础版已满足）
3. 总额精度满足误差 <= 0.01，且超限有明确告警。（基础版已接近，需测试固化）
4. 关键流程有自动化测试并可稳定回归。（未完成）

## 7. 当前未完成待办项（截至 2026-02-11）

- [x] 待办项：将“字段映射确认”做成可编辑配置（上传明细后可手工映射列名并保存模板），避免列名变化导致解析失败。
- [ ] 待办项：补齐“计费项完整编辑”能力（目前主要是新增、停用、局部字段修改），需要支持整行编辑、批量编辑、版本发布。
- [ ] 待办项：将对账单主数据从 `statements_db.json` 迁移到数据库，统一与配置中心的数据存储，减少一致性风险。
- [x] 待办项：把生成引擎从硬编码改为规则驱动（比例阈值、补齐顺序、层级策略可配置），提升扩展性。（第一版已完成）
- [ ] 待办项：补充导出模板的数字格式策略（数量/单价/金额的精度与显示规则统一配置）。
- [ ] 待办项：支持多模板管理（不同客户/场景选择不同“对账单附件模板”导出）。
- [ ] 待办项：完善审计日志（配置变更、解析结果、生成动作、导出动作）并提供查询入口。（暂不做）
- [ ] 待办项：补充自动化测试（配置解析、明细解析、生成引擎、导出模板填充、API集成回归）。

## 8. 新增改造目标（待确认后开发）

- [x] 目标A：结算层职责重构为“L1固定、L2/L3/L4模拟”。（基础版已落地）
- [x] 规则A1：L1 作为固定费用层，仅按 `单价 × 业务数量` 入账；金额不参与模拟补齐，不参与尾差调整。（基础版已落地）
- [x] 规则A2：L2/L3/L4 作为可调节层，仅对 `can_simulate=true` 的计费项进行数量生成与差额调整。（基础版已落地）
- [x] 规则A3：对 `can_simulate=false` 的计费项一律按业务真实数量直接计算，不允许在平衡阶段改数量。（基础版已落地）

- [ ] 目标B：报告数据优先核对与证据化。（进行中）
- [x] 规则B1：识别“报告类”业务数据并建立优先校验清单（匹配率、数量、金额、占比）。（基础版：已识别报告支撑金额与占比）
- [x] 规则B2：报告类校验结果写入生成告警/解释日志，作为对账单可追溯依据。（基础版：已写入 warnings）
- [x] 规则B3：若缺少报告支撑数据，差额统一归入 `L3-知识付费`（可配置默认计费项）进行结算解释。（基础版已落地，默认回落 L3）

- [x] 目标C：引擎可行性预检与失败可解释。（基础版已落地）
- [x] 规则C1：生成前先做可行性校验（如 L1 固定金额超过目标金额时直接阻断并给出明细原因）。（基础版已落地）
- [x] 规则C2：失败信息需包含：固定金额、可模拟容量、不可模拟差额、推荐处理动作（调价/调目标/补报告）。（基础版已落地）

- [ ] 目标D：配置中心支撑新规则。
- [ ] 规则D1：在“系统设置-计费项配置”中补充“报告类标记/证据类型/默认归因层级”等字段（如采用最小实现可先放规则模板）。
- [ ] 规则D2：规则模板新增“无报告回落策略”（默认回落 L3 知识付费）并支持版本化。

- [ ] 验收标准：
- [ ] 1) 当仅有业务固定费用时，系统不会尝试通过 L1 调整金额，且失败提示可解释。
- [ ] 2) 当报告数据缺失时，系统自动将可解释差额归集到 L3 指定项并保留告警说明。
- [ ] 3) 生成结果可追溯“固定项金额”“模拟项金额”“报告核对结果”“回落逻辑”四类证据。

## 9. V2.0 本期开发计划目标（待确认后开始开发）

目标说明：本期以 `PDR/结算引擎需求V2.0.md` 为唯一实现基线；v1.0 仅保留与 V2.0 不冲突的通用能力（如可追溯、导出、失败可解释）。

### 9.1 依赖关系与执行顺序（P0）

依赖链（必须按顺序推进）：

`P0-1（数据模型） -> P0-2（字段迁移） -> P0-3（ACTUAL_SELECTABLE 算法） -> P0-4（流程重构） + P0-5（快照）`

`P0-6（预检增强）` 依赖 `P0-4/P0-5`，在流程与快照落地后实现。

### 9.2 Phase 划分（本期）

#### Phase P0-A：模型与兼容基座（先完成）

- [ ] P0-1：去除 `must_use` 计算语义，所有计费项改为“策略启停可选”。
- [ ] P0-2：落地 `quantity_mode` 四模式：`ACTUAL_FULL` / `ACTUAL_SELECTABLE` / `SIMULATED` / `MANUAL`。
- [ ] Task A1：扩展 schema（计费项/策略模板）字段：`quantity_mode`、`min_pick_ratio`、`max_pick_ratio`、`pick_priority`、`enabled_item_ids`、`tail_diff_threshold`、`fallback_l3_item_id`、`max_simulation_ratio`。
- [ ] Task A2：字段迁移与兼容映射：`can_simulate -> quantity_mode`（只迁移新单据计算路径）。

#### Phase P0-B：ACTUAL_SELECTABLE 择取算法（最高风险，单列）

- [ ] P0-3：实现 `ACTUAL_SELECTABLE` 择取求解器，满足 `0 <= billed_qty <= actual_qty` 与择取比例约束。
- [ ] Task B1：先实现基础版 `priority_greedy_v1 + bounded_backtracking`，保证可解释与可控时限。
- [ ] Task B2：预留 ILP/DP 插件接口，不在本期作为默认路径。
- [ ] Task B3：边界场景专项（比例冲突、目标不可达、尾差超阈值、优先级冲突）与不可行原因码标准化。
- [ ] 时间预留：Phase P0-B 预留约 50% 工时用于边界问题修复与回归。

#### Phase P0-C：流程重构与解释输出

- [ ] P0-4：重构生成流程为“固定项入账 -> 可择取优化 -> 模拟补位 -> L4尾差调平”。
- [ ] P0-5：补齐可解释结果快照：`calc_snapshot_json`、`item_decision_json`，并输出每项 `actual_qty/billed_qty/unbilled_qty`。
- [ ] Task C1：实现 `TAIL_BALANCE` 唯一承接规则；超 `tail_diff_threshold` 强制回主优化，不允许 L4 大额兜底。

#### Phase P0-D：可行性预检增强（依赖 C）

- [ ] P0-6：新增可行性预检与失败返回标准：误差阈值 `<=0.01`，超阈值直接失败并返回约束/建议动作。
- [ ] Task D1：预检输出与主流程日志字段对齐，避免“预检可行但求解失败”的口径不一致。

### 9.3 历史数据兼容门禁（强制）

- [ ] 兼容门禁-1：旧账单保留旧规则快照，禁止按 V2 规则覆盖重算。
- [ ] 兼容门禁-2：`must_use`、`can_simulate` 保留只读用于历史解析，不参与 V2 新计算。
- [ ] 兼容门禁-3：新增版本标识（如 `engine_version` / `rule_snapshot_version`）区分 V1/V2 账单读取路径。
- [ ] 兼容门禁-4：发布前需完成“历史账单可读性回归”与“新旧账单并存回归”。

### 9.4 本期范围（P1，建议同迭代完成）

- [ ] 目标P1-1：系统设置页补齐 V2.0 配置项编辑能力（`quantity_mode`、择取区间、尾差承接标记、报告要求）。
- [ ] 目标P1-2：新建对账单页增加“可行性评估 + 择取预览（实际/择取/未取）”。
- [ ] 目标P1-3：详情页新增“计算说明”与“尾差调平明细”展示。

### 9.5 明确不做（本期排除）

- [ ] 审批流完整状态机与多级审批（延后）。
- [ ] 审计查询中心（延后；仅保留必要结果快照与生成日志）。
- [ ] PDF 模板深度定制（延后；维持现有导出能力）。

### 9.6 本期验收标准（DoD）

- [ ] DoD-1：停用计费项不进入结算明细，启用项按策略参与。
- [ ] DoD-2：`ACTUAL_SELECTABLE` 项满足 `0 <= billed_qty <= actual_qty` 且可输出未取数量。
- [ ] DoD-3：总金额与目标误差 `<=0.01`；超限场景返回可解释失败而非静默兜底。
- [ ] DoD-4：尾差仅进入 L4 `TAIL_BALANCE`；超阈值不得用 L4 大额吞差。
- [ ] DoD-5：每张账单可追溯输入约束、择取过程、回退原因与最终决策快照。
- [ ] DoD-6：历史账单按旧规则可读且不可被 V2 隐式重算覆盖。

## 10. 金额凑单失败专项修复清单（2026-02-12 新增）

目标：解决当前“计费项数量无法灵活取用 + 大量 `TAIL_DIFF_EXCEEDED` 阻断”问题，达成“可凑金额 + 结果严格等于目标金额”。

### 10.1 P0（必须先完成，阻断级）

- [x] P0-1：修复运行模式优先级（状态：已完成，2026-02-12）
- [x] 具体：引擎计算优先使用 `quantity_mode`，仅在其为空/非法时回退 `billing_mode` 映射；禁止 `billing_mode` 覆盖已存在 `quantity_mode`。
- [x] 模块：`backend/logic.py`（`load_config`、`_normalize_quantity_mode` 调用链）。
- [x] 验收：`SIMULATED` 项在运行态仍为 `SIMULATED`，不再被错误识别为 `ACTUAL_SELECTABLE`。

- [x] P0-2：修复历史配置脏数据（状态：已完成，2026-02-12）
- [x] 具体：对 `billing_items` 执行一次性回填：`quantity_mode=SIMULATED` 的记录，`billing_mode` 必须回填为“模拟填充”；生成迁移脚本并可重复执行。
- [x] 模块：`backend/data/app.db` 迁移脚本 + 启动迁移入口。
- [x] 验收：迁移后“模式不一致”记录数为 0（以脚本校验为准）。

- [x] P0-3：恢复模拟补位能力（状态：已完成，2026-02-12）
- [x] 具体：确保模拟池包含 L3/L4 合法项；补位阶段确实执行 `SIMULATION_FILL_GAP`，不再长期为 0。
- [x] 模块：`backend/logic.py`（`_bounded_backtracking` 非超目标约束，避免择取阶段先超目标）、`simulated_items`/`_allocate_simulation` 路径联动恢复。
- [x] 验收：同一测试文件下，历史 `UNSPECIFIED` 失败样本（如 `10011`、`50011`）已可成功补位并严格对齐目标金额。

- [x] P0-4：保持严格金额一致（状态：已完成，2026-02-12）
- [x] 具体：保留最终金额强校验，目标与生成金额必须一致；失败必须返回可解释原因码；新增 P0-4 门禁回归测试（固定样本 404 组）。
- [x] 模块：`backend/logic.py`（`final_diff` 校验段）+ `precheck` + `backend/tests/test_regression.py`。
- [x] 验收：固定测试区间（`1000-1100`、`5000-5100`、`10000-10100`、`50000-50100`）全部通过且 `generated_amount == target_amount`（严格 0 差异）。

### 10.2 P1（能力增强，减少误阻断）

- [ ] P1-1：尾差阈值策略改造（状态：待做）
- [ ] 具体：阈值由“固定值”升级为“可调粒度动态阈值”：`max(config_threshold, min_tail_unit)`，并增加上限保护。
- [ ] 模块：`backend/logic.py`（`_derive_tail_threshold`）。
- [ ] 验收：存在 1 元尾差项时，不再因固定 1.00 阈值导致大面积可调场景被误阻断。

- [ ] P1-2：增加“正负尾差抵消”（状态：待做）
- [ ] 具体：同客户同周期批次生成时，先做净额抵消，再判定 `TAIL_DIFF_EXCEEDED`。
- [ ] 模块：批量生成服务层（新增批次聚合校验模块）。
- [ ] 验收：批次净额可归零场景不再被单据级尾差误判失败。

- [ ] P1-3：增加“多笔合并判断”（状态：待做）
- [ ] 具体：支持批次级金额一致性判断开关；单单据小偏差在批次总额严格一致前提下可通过。
- [ ] 模块：生成流程编排层 + 可行性预检。
- [ ] 验收：批次总额与目标总额严格一致，且每条记录保留合并路径解释。

### 10.3 P2（测试与可观测性，防回归）

- [ ] P2-1：新增专项回归测试集（状态：待做）
- [ ] 具体：固定输入 `PRD/测试业务使用数据.xlsx`，批量扫描 `1000-1100`、`5000-5100`、`10000-10100`、`50000-50100`。
- [ ] 模块：`backend/tests/test_regression.py` + 新增专项测试文件。
- [ ] 验收：回归报告包含成功率、失败原因分布、金额严格一致率。

- [ ] P2-2：新增“模式一致性”守护测试（状态：待做）
- [ ] 具体：断言 DB 的 `quantity_mode` 与运行态模式一致；新增启动时自检告警。
- [ ] 模块：配置加载与测试层。
- [ ] 验收：不允许再出现“原始 SIMULATED、运行态 ACTUAL_SELECTABLE”。

- [ ] P2-3：增强可解释快照（状态：待做）
- [ ] 具体：快照增加 `mode_source`、`sim_pool_size`、`tail_item`、`block_reason` 字段，便于快速定位阻断环节。
- [ ] 模块：`calc_snapshot_json` 输出结构。
- [ ] 验收：单据失败时可直接从快照定位到具体阻断步骤与阈值依据。

### 10.4 本专项验收口径（强制）

- [x] A：计费项数量灵活取用达标。定义：不要求每个项目用完，系统可按目标金额择取数量并稳定成单。（2026-02-12：固定测试区间验证通过）
- [x] B：金额严格一致达标。定义：所有成功单据必须满足 `generated_amount == target_amount`。（2026-02-12：固定测试区间验证通过）
- [x] C：批量可行性达标。定义：在固定测试区间内，失败率显著下降且失败原因可解释、可行动。（2026-02-12：404/404 可行，失败 0）
